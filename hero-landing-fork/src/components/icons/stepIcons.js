import {
  ClipboardList,
  Eye,
  Layers,
  Printer,
  Timer,
  Truck,
} from 'lucide-react';

export const stepIcons = {
  eye: Eye,
  layers: Layers,
  timer: Timer,
  clipboard: ClipboardList,
  printer: Printer,
  truck: Truck,
};
